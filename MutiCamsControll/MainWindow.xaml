<Window x:Class="MultiCamsControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiCamsControl"
        xmlns:viewModel="clr-namespace:MultiCamsControl.ViewModel"
        xmlns:converters="clr-namespace:MultiCamsControl.Converters"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Multi-Camera Control" Height="800" Width="1200"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        d:DataContext="{d:DesignInstance Type=viewModel:Func1ViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:BooleanToOppositeConverter x:Key="BooleanToOppositeConverter" />
            <converters:NullToFalseConverter x:Key="NullToFalseConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16">
            <StackPanel Orientation="Horizontal">
                <Button 
                        Command="{Binding StartWorkflowCommand}"
                        ToolTip="Start Workflow"
                        
                        Background="{DynamicResource SecondaryHueMidBrush}"
                        BorderBrush="{DynamicResource SecondaryHueMidBrush}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanToOppositeConverter}}">
                    <materialDesign:PackIcon Kind="Play"/>
                </Button>

                <ComboBox Margin="16,0"
                          
                          materialDesign:HintAssist.Hint="Select Camera Group"
                          ItemsSource="{Binding CameraGroups}"
                          SelectedItem="{Binding SelectedCameraGroup}">
                </ComboBox>

                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding SwitchCameraGroupCommand}"
                        Content="Switch Group"
                        IsEnabled="{Binding SelectedCameraGroup, Converter={StaticResource NullToFalseConverter}}"/>

                <TextBlock Text="{Binding Status}" VerticalAlignment="Center" Margin="16,0"/>
            </StackPanel>
        </materialDesign:ColorZone>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" Margin="8"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="350" Margin="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="200"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Image Source="{Binding Image}" Stretch="Uniform" Margin="8"/>
                                <DataGrid Grid.Row="1"
                                          ItemsSource="{Binding Predictions}"
                                          CanUserSortColumns="True"
                                          CanUserAddRows="False"
                                          AutoGenerateColumns="False"
                                          materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                                          materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Class" Binding="{Binding ClassName}" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence, StringFormat='P2'}" IsReadOnly="True"/>
                                        <DataGridTextColumn Header="BoundingBox" Binding="{Binding BoundingBox}" IsReadOnly="True"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="0" Grid.RowSpan="2" Background="#80000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True"/>
        </Grid>
    </Grid>
</Window>